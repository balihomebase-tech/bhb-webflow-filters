# BHB Webflow Filters

A modular, production-ready filtering system for Bali Home Base property listings. Three independent filters (Rentals, Villas, Land) with shared utilities, core logic, and a redesigned architecture for maintainability.

- **Rentals**: Availability-aware rental property filter
- **Villas**: Ownership-based villa filter with dynamic price ranges
- **Land**: Land size and price-per-are calculations

---

## Quick Start

Add these scripts to your Webflow page (in order):

### For Rentals Page
```html
<!-- Config + Utils + Core → Page Script -->
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/configs/rentals-config.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/utils/currency.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/utils/map.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/core/filter-engine.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/pages/rentals.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/css/filter.css">
```

### For Villas Page
```html
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/configs/villas-config.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/utils/currency.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/utils/map.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/core/filter-engine.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/pages/villas.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/css/filter.css">
```

### For Land Page
```html
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/configs/land-config.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/utils/currency.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/utils/map.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/core/filter-engine.js"></script>
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/js/pages/land.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@main/css/filter.css">
```
  
Replace `USERNAME` with your GitHub username.

> **⚠️ Script order is critical**: Config → Utils → Core → Page. Utilities must load before the page script initializes.

## Features by Page

| Feature | Rentals | Villas | Land |
|---------|---------|--------|------|
| **Price Range** | Dynamic from CMS | Dynamic (based on ownership) | Dynamic from CMS (per are) |
| **Price Slider** | ✅ | ✅ | ✅ |
| **Currency Switcher** | IDR/USD/EUR | IDR/USD/EUR | IDR/USD/EUR |
| **Location Map** | ✅ MapTiler | ✅ MapTiler | ✅ MapTiler |
| **Keyword Search** | ✅ | ✅ | ✅ |
| **Load More** | ✅ | ✅ | ✅ |
| **Availability Filter** | ✅ (Available/Rented) | — | — |
| **Bedrooms Filter** | ✅ (1-6+) | ✅ (1-6+) | — |
| **Ownership Filter** | — | ✅ (Leasehold/Freehold) | ✅ (Leasehold/Freehold) |
| **Lease Duration** | — | ✅ | ✅ |
| **Land Size Filter** | — | — | ✅ |
| **Price Per Are** | — | — | ✅ |

---

## Setting Up in Webflow

### 1. Create Required DOM Elements

**Grid container** (with proper ID):
```html
<div id="rentals-wrapper" class="w-dyn-list">
  <!-- Webflow dynamic list here -->
</div>
```

**Filter panel**:
```html
<form class="rent-filter_form-block">
  <!-- Filter controls: ownership, bedrooms, location, lease, currency, etc. -->
</form>
```

**Price slider elements**:
```html
<div id="pwFill" class="pw-fill"></div>
<div class="pw-slider">
  <!-- Thumbs and controls generated by script -->
</div>
<input type="text" id="pwMinText">
<input type="text" id="pwMaxText">
<div id="pwRangeText"></div>
<div id="pwScaleMin"></div>
<div id="pwScaleMax"></div>
```

**Map container** (inside location dropdown):
```html
<div id="bhbMap" style="width: 100%; height: 400px;"></div>
```

**Result counters & buttons**:
```html
<span id="rental-results-count">0</span>
<div id="rental-empty-state">No results found</div>
<button id="load-more">Load More</button>
```

### 2. Set Data Attributes on Cards

Each property card needs specific data attributes. Set these in Webflow CMS.

**Rentals** (`.listings_card-wrapper`):
```html
<div class="listings_card-wrapper"
     data-name="Villa Mahalo"
     data-code="MAH-001"
     data-location="Canggu"
     data-rooms="3"
     data-price="75000000"
     data-currency="IDR"
     data-available-date="2024-01-15">
  <!-- Card content -->
</div>
```

**Villas** (`.listings_card-wrapper`):
```html
<div class="listings_card-wrapper"
     data-name="Luxury Sunset Villa"
     data-code="LSV-042"
     data-location="Uluwatu"
     data-rooms="4"
     data-price="85000000"
     data-currency="IDR"
     data-available="Leasehold">
  <!-- Lease year in: <div class="leasehold-year-container"><span class="u-txt-bold">25</span></div> -->
</div>
```

**Land** (`.listings_card-wrapper`):
```html
<div class="listings_card-wrapper"
     data-name="Riverside Plot"
     data-code="RVR-007"
     data-location="Tabanan"
     data-size="500"
     data-price-are="12000000"
     data-currency="IDR"
     data-available="Freehold">
  <!-- Lease year in: <div class="leasehold-year-container"><span class="u-txt-bold">10</span></div> -->
</div>
```

---

## Codebase Structure

```
bhb-webflow-filters/
├── js/
│   ├── configs/
│   │   ├── rentals-config.js    # Rentals: dynamic price calculation from CMS
│   │   ├── villas-config.js     # Villas: dynamic calculation based on ownership
│   │   └── land-config.js       # Land: dynamic price-per-are calculation
│   │
│   ├── core/
│   │   └── filter-engine.js     # Shared: applyFilters(), passes(), UI updates
│   │
│   ├── pages/
│   │   ├── rentals.js           # Rentals-specific: init, passes, buildAreas
│   │   ├── villas.js            # Villas-specific: dynamic price range logic
│   │   └── land.js              # Land-specific: land size filtering, dynamic pricing
│   │
│   └── utils/
│       ├── currency.js          # norm(), convertAmount(), symFor(), etc.
│       └── map.js               # initMap(), syncMapWith(), makePin(), etc.
│
├── css/
│   └── filter.css               # Shared styles for all filters
│
└── README.md
```

---


### Filter Pipeline

1. **User selects filter criteria** (location, price, availability, ownership, etc.)
2. **`applyFilters()` runs** (from core/filter-engine.js)
3. **Each card is checked** via page-specific `passes()` function
4. **Results are updated** with pagination (load more)
5. **Map markers refresh** to show matching locations
6. **UI updates** (result count, empty state, buttons)

---

## Price Filter Components

The price filtering system is divided into **three UI sections**:

### QUICK SELECTION

Preset chips automatically generated from CMS price data. These divide the price range into three equal tiers for quick filtering.

**Workflow**:
1. System calculates min/max prices from CMS listings
2. Divides range by 3 to create three equal tiers
3. Generates chips labeled with formatted price ranges
4. User clicks a chip to filter to that price tier

### CUSTOM RANGE

User-adjustable slider allowing manual min/max selection. The slider always reflects the current price range calculated from CMS data.

**Workflow**:
1. System calculates min/max prices from CMS listings
2. Slider bounds are set to these values
3. User drags handles to select custom price range
4. Filter applies in real-time as user adjusts

### PRICE RANGE

The calculated minimum and maximum values derived from CMS listings. This range varies depending on:

- **Rentals**: All rental listings in CMS (monthly rental prices)
- **Villas**: Listings matching the current ownership filter (Freehold, Leasehold, or All)
- **Land**: All land listings in CMS (price-per-are values)

---

## Price Filtering by Page

All three pages calculate dynamic price ranges directly from CMS data. Price chips are generated dynamically by dividing the min/max range into thirds:

### Rentals

**Data Source**: Monthly rental prices from all rental listings in CMS.

**Range Calculation**:
- Scans all rental cards
- Extracts monthly rental prices
- Converts all prices to IDR (base currency)
- Calculates min/max from the dataset

**Quick Selection**: Auto-generated chips divide the rental price range into thirds.

**Example**:
```
Min: Rp15jt (monthly)
Max: Rp150jt (monthly)
Range: Rp135jt

Quick Selection chips:
< Rp45jt
Rp45jt – Rp90jt
> Rp90jt
```

**Behavior**:
- Price range reflects **all available rentals**
- No filtering by ownership or other criteria affects price range
- Chips update when currency changes
- Slider bounds always match CMS data

### Villas

**Data Source**: Villa prices from CMS listings, filtered by ownership type.

**Range Calculation**:
- Filters villas by current **Ownership** setting (Any/Freehold/Leasehold)
- Extracts prices from matching villas
- Converts all prices to IDR (base currency)
- Calculates min/max from the filtered dataset
- **Re-calculates when ownership filter changes**

**Quick Selection**: Auto-generated chips divide the villas' price range into thirds.

**Ownership Behavior**:

If ownership = **Any**
```
Price range = min/max from ALL villas in CMS
Example: Rp500jt – Rp5B (all property types)
```

If ownership = **Freehold**
```
Price range = min/max from freehold villas only
Example: Rp800jt – Rp3B (freehold properties)
Quick Selection: < Rp1.4B | Rp1.4B–Rp2.3B | > Rp2.3B
```

If ownership = **Leasehold**
```
Price range = min/max from leasehold villas only
Example: Rp500jt – Rp1.5B (leasehold properties)
Quick Selection: < Rp833jt | Rp833jt–Rp1.17B | > Rp1.17B
```

**Behavior**:
- Price range **changes when ownership filter changes**
- Slider and chips are recalculated immediately
- No delay; updates are synchronous
- Reflects only villas matching the current ownership type

### Land

**Data Source**: Land price-per-are values from CMS listings.

**Range Calculation**:
- Scans all land cards
- Extracts **price-per-are** values (not total price)
- Converts all prices to IDR (base currency)
- Calculates min/max per-are values

**Quick Selection**: Auto-generated chips divide the per-are price range into thirds.

**Example**:
```
Min: Rp300jt / are
Max: Rp900jt / are
Range: Rp600jt

Quick Selection chips:
< Rp500jt / are
Rp500jt – Rp700jt / are
> Rp700jt / are
```

**Behavior**:
- Price range reflects **all available land plots**
- Currency conversion applies to per-are values
- Slider bounds match actual per-are pricing in CMS
- Users can filter by price-per-are, land size (m²), and other criteria independently

---

## Configuration

### Area Groupings

All three pages use the same area structure:

| Area | Locations |
|------|-----------|
| **Canggu area** | Canggu, Pererenan, Seseh, Cemagi, Buduk, Kaba Kaba, Kaba-Kaba, Cepaka, Tumbak Bayuh, Buwit, Dalung |
| **Uluwatu area** | Bingin, Uluwatu, Uluwatu Center, Ungasan |
| **Ubud area** | Ubud, Ubud Center |
| **Tabanan area** | Kedungu, Nyanyi, Pandak Gede, Nyambu, Tanah Lot |

To customize, edit `js/configs/{page}-config.js`:

```javascript
var AREA_RULES = [
  {
    id: "canggu-area",
    label: "Canggu area",
    keys: ["canggu", "pererenan", "seseh", "cemagi", "buduk", ...]
  },
  // ... more areas
];

var LOC_COORDS = {
  canggu: [115.1365, -8.65062],
  pererenan: [115.12346, -8.64904],
  // ... more locations with [longitude, latitude]
};
```

### Price Chips (Dynamic Generation)

Price chips (Quick Selection buttons) are automatically generated from CMS data using a **three-tier division algorithm**:

**Algorithm**:
```
1. Calculate range = max_price - min_price
2. Divide by 3:
   - Tier 1: < (min + range/3)
   - Tier 2: (min + range/3) to (min + 2×range/3)
   - Tier 3: > (min + 2×range/3)
3. Convert to each currency (IDR/USD/EUR)
4. Format labels with currency symbol
```

**Example**: Rentals ranging from Rp15jt to Rp150jt:
```
Range = Rp135jt
Tier 1 = Rp15jt + (Rp135jt / 3) = Rp60jt
Tier 2 = Rp15jt + (2 × Rp135jt / 3) = Rp105jt

Generated chips:
< Rp60jt
Rp60jt – Rp105jt
> Rp105jt
```

**No Static Presets**: 
The `CHIP_PRESETS` object in config files serves only as a fallback. All active chips are dynamically calculated from your CMS data:

```javascript
var CHIP_PRESETS = {
  IDR: [
    { label: "< 50jt", min: 0, max: 50000000 },      // fallback only
    { label: "50jt – 200jt", min: 50000000, max: 200000000 },
    { label: "> 200jt", min: 200000000, max: null }
  ],
  // Actual chips displayed come from generateDynamicChips() calculation
};
```

**When Chips Update**:
- On page load: Calculated from all CMS listings
- On currency change: Converted to new currency, labels updated
- On Villas ownership change: Recalculated from filtered dataset
- In real-time as CMS data changes (if dynamic CMS updates used)

### Grid & Card Settings

Edit `CFG` in `js/configs/{page}-config.js`:

```javascript
var CFG = {
  GRID_ID: "rentals-wrapper",    // ID of grid container
  CARD_SEL: ".w-dyn-item",       // Selector for each card
  STEP: 9                         // Cards to load per "Load More" click
};
```

### Map Settings

Edit these in `js/configs/{page}-config.js`:

```javascript
var MAPTILER_KEY = "your-api-key-here";
var MAP_STYLE = "019c8e23-ebd1-7221-bd5f-20ae2dca2ab6";
var PIN_URL = "https://cdn.prod.website-files.com/.../Icon.svg";
var FILTER_PANEL = ".rent-filter_form-block";  // or .villas-filter_form-block
```

---

## Required DOM IDs & Classes

| Element | ID / Class | Pages |
|---------|-----------|-------|
| **Grid** | `id="rentals-wrapper"` | Rentals |
| | `id="villas-wrapper"` | Villas |
| | `id="lands-wrapper"` | Land |
| **Filter form** | `.rent-filter_form-block` | Rentals, Land |
| | `.villas-filter_form-block` | Villas |
| **Results** | `id="rental-results-count"` | All |
| | `id="rental-empty-state"` | All |
| **Pagination** | `id="load-more"` | Rentals, Land |
| | `.villas-load-more` | Villas |
| **Map** | `id="bhbMap"` | All |
| **Slider** | `id="pwFill"` | All |
| | `.pw-slider` | All |
| | `id="pwMin"`, `id="pwMax"` | All |
| | `id="pwMinText"`, `id="pwMaxText"` | All |
| | `id="pwRangeText"` | All |
| | `id="pwScaleMin"`, `id="pwScaleMax"` | All |

---

## Currency Support

The filters support IDR, USD, and EUR with real-time conversion.

**Integration with your currency switcher**:

```javascript
// Call these from your global currency switcher
window.debugCurrency.convertAmount(amount, fromCurrency, toCurrency);
window.debugCurrency.setCurrency(newCurrency);
```

The filters automatically re-render prices and update the slider when currency changes.

---

## Development & Updating

### Local Development

1. Clone/fork the repository
2. Edit files in your local `js/` directory
3. Test changes in Webflow (use local URLs or staging CDN)
4. Commit changes to `main`
5. Create release tags for stable versions

### Publishing Updates

```bash
# Make your changes and commit
git add .
git commit -m "feat: description of changes"

# Create a release tag
git tag v1.2.3
git push origin main --tags
```

Update Webflow script tags to use the new tag:
```html
<!-- Instead of @main, use specific version -->
<script src="https://cdn.jsdelivr.net/gh/USERNAME/bhb-webflow-filters@v1.2.3/js/pages/rentals.js"></script>
```

jsDelivr will cache the @main version aggressively, so versioned tags are recommended for production.

### Common Updates

| Task | File to Edit |
|------|--------------|
| Add/change locations | `js/configs/{page}-config.js` → `AREA_RULES` & `LOC_COORDS` |
| Update price presets | `js/configs/{page}-config.js` → `CHIP_PRESETS` |
| Fix filter logic | `js/pages/{page}.js` |
| Adjust map behavior | `js/utils/map.js` |
| Add new filter type | `js/pages/{page}.js` + `core/filter-engine.js` |
| Style changes | `css/filter.css` |

---

## Troubleshooting

**Filters not loading?**
- Check console for errors
- Verify all script tags are in correct order
- Confirm grid ID matches `CFG.GRID_ID`

**Price range incorrect?**
- Check `data-currency` attributes on cards
- For Villas: ensure `data-available` is set (Leasehold/Freehold)
- Verify prices are numeric values

**Map not showing?**
- Ensure `id="bhbMap"` exists in DOM
- Check MapTiler key is valid
- Confirm location has coordinates in `LOC_COORDS`

**Currency conversion not working?**
- Ensure `window.debugCurrency` is defined
- Call `window.debugCurrency.setCurrency("IDR")` from your switcher

**Load more button not working?**
- Rentals: Check `id="load-more"` exists
- Villas: Check `.villas-load-more` class exists
- Land: Check `id="load-more"` exists

---

## Browser Support

- Modern browsers with ES5+ support
- Requires DOM APIs: `querySelector`, `classList`, `dataset`
- MapTiler SDK (auto-loaded via CDN)
- localStorage for currency persistence

---

## Technical Details

### Script Dependencies

```
Page Script
  ├── Core Filter Engine
  ├── Utils → Currency
  ├── Utils → Map
  └── Config (AREA_RULES, LOC_COORDS, CHIP_PRESETS, etc.)
```

### Global State

Each page maintains a `state` object:
```javascript
{
  ownership: "Any",        // Rentals: ignored, Villas/Land: Leasehold/Freehold
  bedrooms: [],            // Rentals/Villas: [1, 2, 3, ...]
  lease: "Any",            // Villas/Land: lease duration
  locations: [],           // All: selected location names
  currency: "IDR",         // All: current currency
  priceMin: null,          // All: minimum price filter
  priceMax: null,          // All: maximum price filter
  keyword: ""              // All: search query
}
```

### Filter Order

Filters are applied with AND logic:
1. Location (if selected)
2. Ownership (if selected)
3. Bedrooms (if selected)
4. Lease duration (if selected)
5. Price range
6. Keyword search
7. Page-specific filters (availability, land size, etc.)

---

## No Build Step Required

This is vanilla JavaScript with no dependencies or build process. Simply include the scripts in your Webflow page and the filters initialize automatically.

---

## License & Credit

© Bali Home Base. All rights reserved.

Created for [balihomebase.com](https://balihomebase.com)